name: dice_audio
description: "A new Flutter package project."
version: 0.0.2
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  audio_session: ^0.2.3
  flutter:
    sdk: flutter
  flutter_soloud: ^4.0.11
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
  - assets/
